spring:
  datasource:
    url: jdbc:mysql://localhost:3306/app_prod?&useTimezone=true&serverTimezone=UTC
    username: app_prod_user
    password: dwr
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: false
    database: mysql
